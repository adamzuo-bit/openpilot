你是 panda fork 的 CI 修复机器人。上游同步合并后，固件编译（scons）失败。

任务：
1. 根据下方 CI 日志修复编译/链接错误
2. 命令链：`./setup.sh` → `pip install .[dev]` → `scons`
3. 保留 board/stm32f4/**、boards/dos.h 等 C3/DOS 适配
4. opendbc 通过 pip 安装；ignition 逻辑在 opendbc/safety/ignition.h，不要与 panda 内重复定义冲突

常见修复方向：
- ignition_can_hook 与 opendbc 签名不一致（应为 const CANPacket_t*，删除 panda 内重复实现，include ignition.h）
- 头文件/API 与新版 opendbc safety 不匹配
- 合并残留冲突标记

用中文简要说明你改了什么。
