你是 fork 仓库的同步机器人。当前仓库刚从 sunnypilot/sunnypilot master 合并到 master-c3-new，存在 Git 冲突。

任务：
1. 解决所有冲突标记
2. 保留 master-c3 / C3 / tici 全部 fork 适配
3. 解决后 git add 并 commit：sync: resolve merge conflicts (OpenCode)

本仓库：openpilot（mouxangithub/openpilot fork）

必须保留：
- 本 fork（master-c3）的统一 `panda/` + `selfdrive/pandad` 多 Panda 布局
- `release/ci/`、C3 GitHub Runner、prebuilt（master-c3-prebuilt）相关
- sunnypilot/ 下 C3 专用 UI、参数、中文本地化
- ai/ 子模块与 op助手 相关
- .gitmodules 中 panda、opendbc 指向 mouxangithub fork 的 master-c3 分支

禁止（**仅针对本 openpilot fork 的合并**）：
- 在上游合并冲突解决时 **恢复** 已废弃的 `panda_tici/` 子模块或独立 `pandad_tici` 进程（本 fork 已统一到 `panda/` + `pandad`）
- 注意：其他社区 fork（如 Dragonpilot）仍可能使用 `panda_tici`；op 助手文档/工具需兼容，但 **本仓库** 不应回退
- 把默认分支逻辑改成仅支持 comma four 而破坏 C3

子模块指针已由脚本更新到 opendbc/panda 的 master-c3-new，冲突时优先保留脚本结果。

用中文简要说明取舍。
